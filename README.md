ALFOX 3D Frontend
=================

This is a React + Vite project showcasing a 3D hero and certificate verification UI.

To run locally:
1. npm install
2. npm run dev

Place your logo at public/logo.png (already included — from uploaded image).
Set SHEET_ID and SHEET_NAME in src/verify.js if needed.
